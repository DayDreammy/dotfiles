# ~/.zsh-config/03_prompt.zsh

# 设置 OMZ 主题
# "robbyrussell" 是默认的, 很安全
# "agnoster" 很流行, 但需要 Powerline 字体
ZSH_THEME="robbyrussell"

# 推荐：Powerlevel10k (p10k)
# 它是目前最快、最可定制的主题。
# 如果使用它, 请取消注释下面这行, 并按照 p10k 的指南安装
# ZSH_THEME="powerlevel10k/powerlevel10k"
