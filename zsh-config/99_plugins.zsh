# ~/.zsh-config/99_plugins.zsh

plugins=(
    # --- 必备 ---
    git                     # OMZ 内置的 git 别名和功能
    
    # --- 强烈推荐 (需要单独安装) ---
    zsh-syntax-highlighting # 命令语法高亮
    zsh-autosuggestions     # 根据历史记录自动提示
    
    # --- 按需添加 (示例) ---
    # docker
    # kubectl
    # history
    # fzf (如果你安装了 fzf)
)
